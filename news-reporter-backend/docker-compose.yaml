services:
  news-backend:
    build: .
    container_name: news-reporter-backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
